[package]
name = "store"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
near-sdk = { path = "../../../../near-sdk", features = ["default", "unstable"] }

[workspace]
